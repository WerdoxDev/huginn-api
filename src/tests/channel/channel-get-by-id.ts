import { describe, test } from "bun:test";
import { getLoggedClient } from "../test-utils";

describe("channel-by-id", () => {
   test("channel-by-id-successful", async () => {
      const client = await getLoggedClient();

      // client.channels.get()
   });
});
